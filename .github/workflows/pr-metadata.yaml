name: PR metadata annotation234 234

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Client name'
        default: 'World'
        required: true

jobs:
  annotate-pr:
    environment: LICENSE_SECRETS
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run_JS
        uses: ./
        env:
          LICENSE_PRIVATE_KEY: ${{ secrets.LICENSE_PRIVATE_KEY }}
        with:
          name: ${{ github.event.inputs.name }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Get the output time
        run: echo "The time was ${{ steps.Run_JS.outputs.time }}"
      - name: Get the olicense key
        run: echo "The time was ${{ steps.Run_JS.outputs.licensekey }}"
      - name: echo file
        run: echo ${{ steps.Run_JS.outputs.licensekey }} > license.lic

      - name: Upload license
        uses: actions/upload-artifact@v3
        with:
          name: licenseup
          path: license.lic

      - uses: actions/download-artifact@v3
        id: download
        with:
          name: 'download'
          path: license.lic
